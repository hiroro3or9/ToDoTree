# 段階11 — 吸着・個別色・ミニマル表示

## AIへ渡す依頼

```text
ToDoTreeを新規実装しています。AI_INSTRUCTIONS.mdとPROGRESS.mdを読み、段階11「吸着・個別色・ミニマル表示」だけ実装してください。
下記の資料・実装項目・終了条件に従ってください。1回で長ければ未完了の先頭項目だけを終わらせ、続きをPROGRESS.mdへ残してください。
すでにある実装を使い、全体の作り直しや仕様の変更はしないでください。
ビルドと確認を行い、変更点・確認結果・未解決・次の作業を記録してください。
```

## 読む資料

specs/06-blocks-routing.md の「吸着の計算」、specs/02-visual.md の「配色」「カードと線」、specs/palette.md

キットのルートからの相対パスです。指定された節を優先してください。

## 前提

段階10までの実装がビルドできること。

## 実装項目（上から順に、1つずつでよい）

1. 吸着の候補選びとX/Yの保持を実装する（6 / 10 / 160 / 184）。
2. Altでの解除、破線ガイド、画面外と移動群の除外、Esc・タブ切替での後片付けを実装する。
3. ブロックと線の9色＋既定色、未知IDの保持、状態色の優先を実装する。
4. 24×24のミニマル表示へ切り替え、ラベル・接続・ミニマップ・ヒットテストを更新する。

## 終了条件

- [ ] 倍率を変えても、吸着が働く見た目の距離が変わらない。
- [ ] Escでの取消や動かさなかった移動でUndo・dirty・Redoが壊れない。
- [ ] 色の変更をUndoでき、保存して読み直しても残る。
- [ ] ミニマル表示でも選択と線の接続が正しい。

## 確認の作り方

吸着の候補決定（距離・優先順・除外）はCore側のテストで固定する。

## この段階の終了時

追加したファイル、公開したAPI、実行した確認、未確認のことをPROGRESS.mdへ記録する。
終了条件を満たす前に「完了」と書かない。
