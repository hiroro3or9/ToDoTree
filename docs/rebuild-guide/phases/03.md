# 段階03 — DAGの操作と状態判定

## AIへ渡す依頼

```text
ToDoTreeを新規実装しています。AI_INSTRUCTIONS.mdとPROGRESS.mdを読み、段階03「DAGの操作と状態判定」だけ実装してください。
下記の資料・実装項目・終了条件に従ってください。1回で長ければ未完了の先頭項目だけを終わらせ、続きをPROGRESS.mdへ残してください。
すでにある実装を使い、全体の作り直しや仕様の変更はしないでください。
ビルドと確認を行い、変更点・確認結果・未解決・次の作業を記録してください。
```

## 読む資料

specs/05-planning.md の「着手判定の順序」「進捗・最長経路」、specs/04-data-model.md の「読込時の検証」

キットのルートからの相対パスです。指定された節を優先してください。

## 前提

段階02までの実装がビルドできること。

## 実装項目（上から順に、1つずつでよい）

1. TodoGraphのID索引、Add/Remove/Connect/Disconnect、先行・後続・祖先・子孫を実装する。
2. 重複、自己線、存在しない端点、循環を接続前に拒否する。
3. トポロジカル順（Kahn法）と、Ready/Blocked/InProgress/Done/Cancelledの導出を実装する。
4. 状態遷移とCompletedAt、件数・重み付きの進捗を実装する。

## 終了条件

- [ ] A→B/C→DでAを完了するとB/CがReady、片方だけ完了してもDはBlocked。
- [ ] 取り消した先行は後続を解放するが、完了件数には足さない。
- [ ] 循環を作る接続に失敗したとき、辺も履歴も変わらない。

## 確認の作り方

上の3件をそのままテストにする。状態判定は後の段階で条件が増えるので、順序をテストで固定しておく。

## この段階の終了時

追加したファイル、公開したAPI、実行した確認、未確認のことをPROGRESS.mdへ記録する。
終了条件を満たす前に「完了」と書かない。
