# 段階06 — JSON保存・タブ・自動保存・復旧

## AIへ渡す依頼

```text
ToDoTreeを新規実装しています。AI_INSTRUCTIONS.mdとPROGRESS.mdを読み、段階06「JSON保存・タブ・自動保存・復旧」だけ実装してください。
下記の資料・実装項目・終了条件に従ってください。1回で長ければ未完了の先頭項目だけを終わらせ、続きをPROGRESS.mdへ残してください。
すでにある実装を使い、全体の作り直しや仕様の変更はしないでください。
ビルドと確認を行い、変更点・確認結果・未解決・次の作業を記録してください。
```

## 読む資料

specs/04-data-model.md（全部）

キットのルートからの相対パスです。指定された節を優先してください。

## 前提

段階05までの実装がビルドできること。

## 実装項目（上から順に、1つずつでよい）

1. JsonProjectStoreのJSON設定、読込検証、tmp→Replace/Move、.bakを実装する。
2. WorkspaceViewModelで複数タブ、新規、開く、名前を付けて保存、閉じるを繋ぐ。
3. 5秒の自動保存、未保存タブの退避、settings.jsonとセッション復元を実装する。
4. 保存失敗時にdirtyと保存先を維持する処理と、終了確認を実装する。

## 終了条件

- [ ] 保存して読み直すとID・文字・座標・日時が保たれ、2回目の保存で.bakができる。
- [ ] 未保存タブは退避から、保存済みタブは本体から復元される。
- [ ] 保存の失敗や取消でタブを閉じず、dirtyも消えない。
- [ ] タブごとにUndo・検索・表示位置が独立している。

## 確認の作り方

検証の拒否パターン（重複ID・自己線・存在しない端点・しおりの参照切れ）を1件ずつテストする。

## この段階の終了時

追加したファイル、公開したAPI、実行した確認、未確認のことをPROGRESS.mdへ記録する。
終了条件を満たす前に「完了」と書かない。
