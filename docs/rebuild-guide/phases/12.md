# 段階12 — 受信箱・チェック・作業ブロック・しおり・今日の完了

## AIへ渡す依頼

```text
ToDoTreeを新規実装しています。AI_INSTRUCTIONS.mdとPROGRESS.mdを読み、段階12「受信箱・チェック・作業ブロック・しおり・今日の完了」だけ実装してください。
下記の資料・実装項目・終了条件に従ってください。1回で長ければ未完了の先頭項目だけを終わらせ、続きをPROGRESS.mdへ残してください。
すでにある実装を使い、全体の作り直しや仕様の変更はしないでください。
ビルドと確認を行い、変更点・確認結果・未解決・次の作業を記録してください。
```

## 読む資料

specs/07-work-features.md（全部）、specs/05-planning.md の「今日完了したタスク」、specs/ui-text.md

キットのルートからの相対パスです。指定された節を優先してください。

## 前提

段階11までの実装がビルドできること。

## 実装項目（上から順に、1つずつでよい）

1. 受信箱の追加・配置・削除と、カードのチェックリストを実装する。
2. 手動ブロックと理由、阻害原因の一覧とそこへの移動を実装する。
3. しおりの設置・メモ・復帰・解除と、起動時の復帰を実装する。
4. 現在タブと全プロジェクトの今日完了一覧、5秒更新、既知パスの追跡を実装する。

## 終了条件

- [ ] 受信箱は配置するまで進捗・計画に入らず、全部チェックしてもカードは未完了のまま。
- [ ] 手動ブロックを解除すると元の状態に戻り、完了時は印だけ外れて理由が残る。
- [ ] 非表示のカードに置いたしおりへ戻れ、対象を削除してUndoするとメモも戻る。
- [ ] 閉じたプロジェクトの実績も再起動後に読め、壊れたファイルが他の結果を邪魔しない。

## 確認の作り方

今日の判定（日付またぎ）と、閉じたプロジェクトの読み込み失敗時の扱いをテストする。

## この段階の終了時

追加したファイル、公開したAPI、実行した確認、未確認のことをPROGRESS.mdへ記録する。
終了条件を満たす前に「完了」と書かない。
