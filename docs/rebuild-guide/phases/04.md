# 段階04 — カード描画とCanvas操作

## AIへ渡す依頼

```text
ToDoTreeを新規実装しています。AI_INSTRUCTIONS.mdとPROGRESS.mdを読み、段階04「カード描画とCanvas操作」だけ実装してください。
下記の資料・実装項目・終了条件に従ってください。1回で長ければ未完了の先頭項目だけを終わらせ、続きをPROGRESS.mdへ残してください。
すでにある実装を使い、全体の作り直しや仕様の変更はしないでください。
ビルドと確認を行い、変更点・確認結果・未解決・次の作業を記録してください。
```

## 読む資料

specs/02-visual.md の「カードと線」「座標」、specs/03-interactions.md の「グラフ編集」

キットのルートからの相対パスです。指定された節を優先してください。

## 前提

段階03までの実装がビルドできること。

## 実装項目（上から順に、1つずつでよい）

1. NodeViewModel / EdgeViewModelとGraphViewを作り、224×88のカードと矢印を表示する。
2. 単体選択、Ctrlで複数、Altで矩形選択、背景のpan、選択群のドラッグを実装する。
3. カーソル基準のホイールズーム0.25〜2.5、全体表示、ミニマップを実装する。
4. world/screen変換とヒットテストを1か所にまとめる。線の正確な経路は段階08で仕上げる。

## 終了条件

- [ ] 0.25 / 1 / 2.5倍のどれでもカーソル位置を中心に拡縮し、ドラッグ量が画面と合う。
- [ ] 複数のカードを動かしても相対位置が崩れない。
- [ ] 矩形選択が背景のpanと競合しない。

## 確認の作り方

座標変換（world↔screen）とヒットテストはCore側に置いてテストする。

## この段階の終了時

追加したファイル、公開したAPI、実行した確認、未確認のことをPROGRESS.mdへ記録する。
終了条件を満たす前に「完了」と書かない。
