# 段階09 — ブロックの作成・移動・内部整列

## AIへ渡す依頼

```text
ToDoTreeを新規実装しています。AI_INSTRUCTIONS.mdとPROGRESS.mdを読み、段階09「ブロックの作成・移動・内部整列」だけ実装してください。
下記の資料・実装項目・終了条件に従ってください。1回で長ければ未完了の先頭項目だけを終わらせ、続きをPROGRESS.mdへ残してください。
すでにある実装を使い、全体の作り直しや仕様の変更はしないでください。
ビルドと確認を行い、変更点・確認結果・未解決・次の作業を記録してください。
```

## 読む資料

specs/06-blocks-routing.md の「ブロック」「配置」

キットのルートからの相対パスです。指定された節を優先してください。

## 前提

段階08までの実装がビルドできること。

## 実装項目（上から順に、1つずつでよい）

1. 所属の管理と枠の算出、見出しと選択表示を実装する。
2. Ctrl+G、作成直後の命名、F2、解除、カードの所属変更（Shift+ドラッグ）を実装する。
3. 見出しドラッグでの一括移動と、内部通過点の追従、部分非表示のときの移動不可を実装する。
4. Ctrl+Lをブロック内と全体で振り分け、全体整列では囲みを障害物として扱う。

## 終了条件

- [ ] 見出しを動かしても所属カードの相対位置が保たれ、外部線の通過点は動かない。
- [ ] 作成直後のEscで囲みごと取り消せる。解除してもカードは消えない。
- [ ] 内部整列は外側と視野を変えず、全体整列は囲みの中を動かさない。

## 確認の作り方

枠の算出（左右24・下24・見出し32・間12）と所属変更の拒否条件をテストする。

## この段階の終了時

追加したファイル、公開したAPI、実行した確認、未確認のことをPROGRESS.mdへ記録する。
終了条件を満たす前に「完了」と書かない。
