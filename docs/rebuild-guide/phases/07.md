# 段階07 — 計画・検索・表示範囲

## AIへ渡す依頼

```text
ToDoTreeを新規実装しています。AI_INSTRUCTIONS.mdとPROGRESS.mdを読み、段階07「計画・検索・表示範囲」だけ実装してください。
下記の資料・実装項目・終了条件に従ってください。1回で長ければ未完了の先頭項目だけを終わらせ、続きをPROGRESS.mdへ残してください。
すでにある実装を使い、全体の作り直しや仕様の変更はしないでください。
ビルドと確認を行い、変更点・確認結果・未解決・次の作業を記録してください。
```

## 読む資料

specs/05-planning.md（全部）、specs/ui-text.md の「左パネル」「ステータスバー・メッセージ」

キットのルートからの相対パスです。指定された節を優先してください。

## 前提

段階06までの実装がビルドできること。

## 実装項目（上から順に、1つずつでよい）

1. 最長経路、期限の逆算、完了見込み、点数付きの次にやること上位3件を式どおり実装する。
2. 検索、タグ絞込、完了を隠す、枝の折りたたみ、この枝を選択（Ctrl+Shift+↓）、フォーカス、矢印ナビゲーションを実装する。
3. 左パネルとステータスバーへ計算結果を表示する。
4. 未完了の前提が残る合流先や、共有している枝の表示を確認する。

## 終了条件

- [ ] 期限や見積りを変えると、理由の文言と完了見込みが更新される。
- [ ] 見積り未指定30分、1日4時間、予測ペースの0.25〜3倍制限が式どおり。
- [ ] 検索や完了非表示だけでは保存データも進捗の分母も変わらない。

## 確認の作り方

点数表・逆算・見込みは境界値（0〜3日、4〜7日、ペース制限の上下限）をテストする。

## この段階の終了時

追加したファイル、公開したAPI、実行した確認、未確認のことをPROGRESS.mdへ記録する。
終了条件を満たす前に「完了」と書かない。
